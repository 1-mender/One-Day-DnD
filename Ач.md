 Давай решать проблемы [High] Ломается базовый quality gate (verify): npm run verify падает из‑за BOM в ShopJoe.jsx, ShopMascotStall.jsx, shopCatalog.js. Это блокирует стабильный CI/CD для UI-изменений.

[High] Визуальная регрессия сейчас невалидна (4/4 fail): npm run e2e:visual упал по join-* и dm-setup-* (size mismatch + large diff). Скриншоты снимаются fullPage: true в e2e-visual.mjs (line 109)-e2e-visual.mjs (line 112), из-за чего высота зависит от контента/плавающих элементов и тесты нестабильны.

[High] Модалки не соответствуют базовой desktop/mobile accessibility: в Modal.jsx (line 7)-Modal.jsx (line 13) нет role="dialog", aria-modal, фокустрапа, закрытия по Escape, возврата фокуса на триггер. На ПК это ухудшает клавиатурную навигацию, на телефоне — предсказуемость поведения.

[Medium] Плавающий переключатель UI перекрывает контент на мобильном: UiVariantSwitcher.jsx (line 43) + vintage.css (line 3914) (position: fixed, высокий z-index). На мобильных скринах он залезает в рабочую область и может перекрывать CTA/контент.

[Medium] Проблема с вертикальным UX в DM Events: DMEvents.jsx (line 280) + vintage.css (line 3931) (.events-list { height: 70vh; overflow: auto; }) создаёт nested scroll и “съедает” полезную высоту при sticky-шапках/баннерах на телефоне.

[Medium] Избыточные глобальные слушатели активности в PlayerLayout: PlayerLayout.jsx (line 173)-PlayerLayout.jsx (line 178). Даже с throttle это лишняя нагрузка на мобильных (скролл/тач/keydown/focus одновременно).

[Medium] Непоследовательная локализация и UI-текст: часть экранов в t(...), часть с хардкодом/смешением RU+EN (Arcade.jsx (line 382), Arcade.jsx (line 424), DMOpsBar.jsx (line 112), DMInfoBlocks.jsx (line 174)). UX ощущается “неровным”.

[Medium] Мало фронтовых тестов именно на UI-поведение: есть только ticketRulesEditor.test.js и arcadeFormatters.test.js. При большом UI это повышает риск регрессий по адаптиву и интеракциям.

[Low] JS-бандл выше целевого бюджета: по npm run perf:report сейчас js_total_bytes=920967 (899.4 KB), а цель в README.md (line 104) — <= 850 KB.

[Low] Много inline-стилей в ключевых экранах: например Inventory.jsx (line 385), Profile.jsx (line 435), Arcade.jsx (line 358). Это усложняет системную адаптацию под phone/PC и поддержку дизайн-системы.
Починить gates: убрать BOM + стабилизировать visual-тесты (фиксированный clip вместо fullPage, скрыть/детерминировать плавающие виджеты в тестах).
Привести Modal к доступному паттерну (role/aria, focus trap, Escape, return focus, body scroll lock).
Пересобрать навигационные слои под breakpoints: на mobile убрать перекрытия (ui-variant-fab), на desktop дать менее навязчивую постоянную навигацию.
Убрать фикс 70vh в DM Events, перейти на calc(100dvh - offset) + единый scroll container.
Дожать i18n-консистентность (Arcade/DMOps/InfoBlocks/Bestiary) и вынести хардкод-строки.
Добавить фронтовые компонентные тесты для критичных flow (модалки, навигация, mobile breakpoints, inventory/arcade actions).    
