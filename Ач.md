Критичные проблемы (по приоритету)

P0: Системная поломка локализации/кодировки (mojibake) в пользовательских экранах
Join.jsx (line 50)
Join.jsx (line 52)
PlayerLayout.jsx (line 215)
Waiting.jsx (line 43)
Notes.jsx (line 40)
Players.jsx (line 62)
arcadeFormatters.js (line 3)
arcadeFormatters.js (line 35)
App.jsx (line 40)
В интерфейсе часть текста нечитаема, часть в escape/битой форме, часть нормальная. Это главный UX-блокер.

P0: Нет единого языкового стандарта интерфейса (RU/EN/технический жаргон вперемешку)
DMSetup.jsx (line 39)
DMTabBar.jsx (line 5)
DMOpsBar.jsx (line 111)
OfflineBanner.jsx (line 8)
DMSettings.jsx (line 356)
Notes.jsx (line 39)
Пользователь постоянно переключает контекст языка и терминов, особенно между DM и Player.

P0: Ошибки показываются “как есть” (коды/сырой текст), а не как UX-сообщения
formatError.js (line 3)
errorCodes.js (line 1)
ErrorBanner.jsx (line 5)
Сейчас request_failed, read_only, backend-коды и сырые строки легко доходят до UI, что ломает понятность.

P1: Перегруженная IA (информационная архитектура) в DM и Player
DMTabBar.jsx (line 4)
PlayerLayout.jsx (line 214)
В DM 8 первичных разделов в одном уровне, у игрока 6-7 пунктов в bottom nav. Навигация широкая, но не приоритизирована по сценариям.

P1: Нативные window.confirm/prompt ломают целостность UX и мобилку
DMEvents.jsx (line 162)
DMEvents.jsx (line 165)
DMSettings.jsx (line 180)
DMPlayers.jsx (line 124)
DMOpsBar.jsx (line 103)
Системные диалоги выбиваются из стиля, плохо масштабируются и хуже по доступности.

P1: Доступность проседает в интеракциях
ToastProvider.jsx (line 35)
ActionMenu.jsx (line 9)
ActionMenu.jsx (line 42)
У toast-кнопки закрытия нет aria-label; у action menu нет полноценного keyboard UX (Esc/навигация стрелками/управление фокусом).

P1: Визуальная и кодовая консистентность низкая из-за большого объема inline-стилей
Аудит: 387 вхождений style={{...}} по client/src.
Это сильно затрудняет масштабирование дизайна и быстрые массовые улучшения mobile/desktop.

P1: Два больших style-слоя одновременно повышают риск каскадных конфликтов
main.jsx (line 5)
main.jsx (line 6)
Глобальный styles.css + большой vintage.css без четкой изоляции компонентов.

P2: Переключение UI-вариантов скрыто от пользователей
main.jsx (line 52)
Смена варианта только через Ctrl+Shift+U; для DM/игроков это не discoverable.

P2: Недостаточно UX-регресс-защиты
package.json (line 8)
Есть lint/test/build, но нет визуального e2e/регресса интерфейса.

Что улучшать в текущем UI/UX (без полного переписывания)

Ввести единый слой локализации (ru словарь) и убрать строки из компонентов.
Починить кодировку всех UI-строк единообразно (UTF-8 без BOM + pre-commit check).
Внедрить errorMapper (backend code -> человеко-понятное сообщение + action hint).
Заменить все window.confirm/prompt на унифицированный ConfirmModal.
Сжать top-level навигацию:
Player: 4 главных пункта + “Ещё”.
DM: 5 главных разделов + вторичная навигация внутри секции.
Вытащить inline-стили в компоненты/utility-классы (сначала hot-path экраны).
Установить минимальный мобильный размер шрифта и target size интеракций.
Добавить видимый переключатель UI-варианта в DM Settings.
Что лучше сделать “по-новой” (перепроектировать)

Новый foundation-слой UI:

tokens (цвет, типографика, spacing, radius, shadow).

Базовые primitives: PageHeader, SectionCard, FilterBar, StatusBanner, EmptyState, ConfirmDialog.

Новый IA-каркас:

Player: сценарии “игра сейчас”, “персонаж”, “ресурсы”, “история”.

DM: двухпанельная рабочая зона (список + контекст справа), на мобилке master-detail.

Новый контент-слой:

Единый tone-of-voice на русском.

Стандартизированные короткие микротексты для статусов/ошибок/подсказок.

Новый QA-контур UX:

smoke e2e по критическим потокам (Join -> Waiting -> App, DM login/setup, Shop/Arcade/Inventory).

Визуальные снимки ключевых экранов (desktop + mobile).

