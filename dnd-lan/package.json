{
  "name": "dnd-lan",
  "private": true,
  "engines": {
    "node": ">=18.18.0"
  },
  "scripts": {
    "dev": "node scripts/dev.js",
    "build": "npm --prefix client run build && node ./server/src/util.js copy-client",
    "start": "npm --prefix server run start",
    "test": "npm --prefix server run test"
  },
  "dependencies": {
    "multer": "^2.0.2"
  }
}
